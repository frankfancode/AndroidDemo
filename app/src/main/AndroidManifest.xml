<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.frankfancode.androiddemo" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LrcActivity"
            android:label="@string/title_activity_lrc"
            android:theme="@style/MyTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".activity.TestAIDLProxyActivity"
            android:label="@string/title_activity_lrc" />
        <activity
            android:name=".activity.DeailCardActivity"
            android:label="@string/title_activity_deail_card" >
        </activity>
    </application>

</manifest>
